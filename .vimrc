" Line Numbers
set relativenumber      " Relative line number
set number              " Show actual line number on highlight

" Tabs
set expandtab           " Convert tabs to spaces
set tabstop=8           " Number of spaces that a tab character represents
set shiftwidth=8        " Number of spaces to use for each indentation
set softtabstop=8       " Number of spaces a tab keypress uses for indenting

" Auto Indents
set autoindent
set smartindent

" Vim-Plug
" call plug#begin()
         " List your plugins here
" call plug#end()
